{
 "suites": [
  "test_tick_ocr.py"
 ],
 "files": {
  "tick_ocr.py": "4cf06f24721a33ba53310a4f9441233b99e6b6a543cabf6c3819d2bd9d97a31f",
  "axis_reader.py": "982407c7cc841d4da4f69b086cb5e6cf59331322e397f7b000241b2ef488f2ca"
 },
 "mutations": [
  {
   "name": "the progression fills an empty row",
   "file": "tick_ocr.py",
   "old": "    usable = [(r, per_row[r]) for r in rows if per_row.get(r)]",
   "new": "    usable = [(r, per_row.get(r) or [0.0]) for r in rows]"
  },
  {
   "name": "the ladder check is relaxed to monotone",
   "file": "tick_ocr.py",
   "old": "        ok = A.ladder(pairs, allow_subset=False)[0]",
   "new": "        ok = all(a[0] > b[0] for a, b in zip(pairs, pairs[1:]))"
  },
  {
   "name": "subsets are allowed, so a misread is dropped",
   "file": "tick_ocr.py",
   "old": "        ok = A.ladder(pairs, allow_subset=False)[0]",
   "new": "        ok = A.ladder(pairs, allow_subset=True)[0]"
  },
  {
   "name": "ties are broken instead of refused",
   "file": "tick_ocr.py",
   "old": "    if len(distinct) > 1:\n        return [], (\"%d different combinations of the values read form a ladder, \"\n                    \"so the axis is not determined by what was read\"\n                    % len(distinct))",
   "new": "    if False:\n        pass"
  },
  {
   "name": "the MIN_LABELS floor is dropped",
   "file": "tick_ocr.py",
   "old": "    if len(usable) < A.MIN_LABELS:",
   "new": "    if len(usable) < 1:"
  },
  {
   "name": "the crop height is a constant",
   "file": "tick_ocr.py",
   "old": "    half = (min(gaps) // 2) if gaps else reach",
   "new": "    half = reach"
  },
  {
   "name": "the crop ignores the label side",
   "file": "tick_ocr.py",
   "old": "    if side == \"LEFT\":\n        left, right = max(0, int(spine_x) - reach), max(0, int(spine_x))\n    else:\n        left, right = min(w, int(spine_x) + 1), min(w, int(spine_x) + 1 + reach)",
   "new": "    left, right = max(0, int(spine_x) - reach), max(0, int(spine_x))"
  },
  {
   "name": "agreeing attempts each count as a candidate",
   "file": "tick_ocr.py",
   "old": "    return sorted(best.values(), key=lambda t: -t[2])",
   "new": "    return sorted(cands, key=lambda t: -t[2])"
  },
  {
   "name": "best_per_value keeps the worst confidence",
   "file": "tick_ocr.py",
   "old": "        if v not in best or c > best[v][2]:",
   "new": "        if v not in best or c < best[v][2]:"
  },
  {
   "name": "the binarisation uses a fixed threshold",
   "file": "tick_ocr.py",
   "old": "                    Image.fromarray(np.where(arr <= t, 0, 255).astype(\"uint8\"))))",
   "new": "                    Image.fromarray(np.where(arr <= 128, 0, 255).astype(\"uint8\"))))"
  },
  {
   "name": "the flag read becomes bool()",
   "file": "tick_ocr.py",
   "old": "ON = os.environ.get(\"TICKOCR\", \"0\") != \"0\"",
   "new": "ON = bool(os.environ.get(\"TICKOCR\", \"0\"))"
  },
  {
   "name": "an empty row is silently skipped",
   "file": "tick_ocr.py",
   "old": "        T.add(\"TICK_OCR\", panel=label, tick_row=y, box=T.box(cb),\n              outcome=(READ if keep else NO_CANDIDATE),",
   "new": "        if not keep:\n            continue\n        T.add(\"TICK_OCR\", panel=label, tick_row=y, box=T.box(cb),\n              outcome=(READ if keep else NO_CANDIDATE),"
  },
  {
   "name": "only one magnification is tried",
   "file": "tick_ocr.py",
   "old": "SCALES = (4, 6, 8)",
   "new": "SCALES = (3,)"
  },
  {
   "name": "only the pass's own ink is tried",
   "file": "tick_ocr.py",
   "old": "    out = [int(ink)]\n    if int(ink) != int(A.INK_DEFAULT):\n        out.append(int(A.INK_DEFAULT))\n    return out",
   "new": "    return [int(ink)]"
  },
  {
   "name": "라벨 띠를 해상도에 맞추지 않는다",
   "file": "axis_reader.py",
   "old": "    if not (k > 1.2):            # 같은 크기면 늘릴 것이 없습니다\n        return out",
   "new": "    if True:\n        return out"
  },
  {
   "name": "늘린 띠를 튜닝된 띠보다 먼저 쓴다",
   "file": "axis_reader.py",
   "old": "    out = list(base)\n    try:",
   "new": "    out = []\n    try:"
  },
  {
   "name": "늘린 띠를 앵커 고리 안에서 덧붙인다",
   "file": "axis_reader.py",
   "old": "    for strips in ([tuned, wider] if wider else [tuned]):",
   "new": "    for strips in [tuned + wider]:"
  },
  {
   "name": "숫자가 아닌 높이에 걸려 넘어진다",
   "file": "axis_reader.py",
   "old": "    except (TypeError, ValueError, ZeroDivisionError):\n        return out",
   "new": "    except ZeroDivisionError:\n        return out"
  },
  {
   "name": "같은 띠를 다시 읽는다",
   "file": "axis_reader.py",
   "old": "        if k not in seen:\n            seen[k] = _ocr_numerals(img, dark, left, right, top, bottom, sc,",
   "new": "        if True:\n            seen[k] = _ocr_numerals(img, dark, left, right, top, bottom, sc,"
  },
  {
   "name": "천 단위 쉼표를 받지 않는다",
   "file": "axis_reader.py",
   "old": "        if re.fullmatch(r\"-?\\d{1,3}(?:,\\d{3})+\", s_):\n            s_ = s_.replace(\",\", \"\")",
   "new": "        if False:\n            s_ = s_.replace(\",\", \"\")"
  },
  {
   "name": "소수점 쉼표를 받지 않는다",
   "file": "axis_reader.py",
   "old": "        elif re.fullmatch(r\"-?\\d+,\\d{1,2}\", s_):\n            s_ = s_.replace(\",\", \".\")",
   "new": "        elif False:\n            s_ = s_.replace(\",\", \".\")"
  },
  {
   "name": "잘린 숫자도 받는다",
   "file": "axis_reader.py",
   "old": "                    pairs = [(v, r) for v, r, cl in trip if not cl]",
   "new": "                    pairs = [(v, r) for v, r, cl in trip]"
  },
  {
   "name": "두 번째 패스가 없다",
   "file": "axis_reader.py",
   "old": "    if ladder(first)[0]:\n        return first\n    reach = tick_reach(",
   "new": "    if True:\n        return first\n    reach = tick_reach("
  },
  {
   "name": "두 번째 패스가 눈금을 지나 시작하지 않는다",
   "file": "axis_reader.py",
   "old": "    reach = tick_reach(dark, spine_x, y0, y1, cap=max(10, (y1 - y0) // 8))\n    second =",
   "new": "    reach = 0\n    second ="
  },
  {
   "name": "두 번째 패스도 x3으로 확대한다",
   "file": "axis_reader.py",
   "old": "    return max(1.0, min(float(scale), scale * k))",
   "new": "    return float(scale)"
  },
  {
   "name": "눈금 닿는 데를 모든 행으로 잰다",
   "file": "axis_reader.py",
   "old": "    ticks = runs[(runs > base + 2) & (runs < sub.shape[1])]\n    return int(np.percentile(ticks, 90)) if ticks.size else base",
   "new": "    ticks = runs[(runs > 0) & (runs < sub.shape[1])]\n    return int(np.percentile(ticks, 90)) if ticks.size else 0"
  },
  {
   "name": "두 번째 패스가 쉼표 없이 읽는다",
   "file": "axis_reader.py",
   "old": "                     scale_for(y1 - y0, scale), comma=True, seen=seen,",
   "new": "                     scale_for(y1 - y0, scale), comma=False, seen=seen,"
  },
  {
   "name": "낱말 왼쪽에 선 것을 보지 않는다",
   "file": "axis_reader.py",
   "old": "            if not s_.startswith(\"-\") and wl - 2 > 0:\n                if dark[mid, max(0, wl - look):wl - 2].any():",
   "new": "            if False:\n                if dark[mid, max(0, wl - look):wl - 2].any():"
  },
  {
   "name": "the sign is dropped at the parse",
   "file": "axis_reader.py",
   "old": "                found[key] = (float(s_), row, c, clipped)",
   "new": "                found[key] = (abs(float(s_)), row, c, clipped)"
  },
  {
   "name": "잘린 숫자가 띠를 통째로 데려간다",
   "file": "axis_reader.py",
   "old": "                    if any(cl == CLIP_STRIP for _v, _r, cl in trip):",
   "new": "                    if any(cl for _v, _r, cl in trip):"
  },
  {
   "name": "가장자리의 잘림도 낱말만 버린다",
   "file": "axis_reader.py",
   "old": "                    if any(cl == CLIP_STRIP for _v, _r, cl in trip):\n                        continue",
   "new": "                    if False:\n                        continue"
  },
  {
   "name": "낱말이 어디서 시작하든 글자를 잃은 것으로 친다",
   "file": "axis_reader.py",
   "old": "                    clipped = CLIP_STRIP if wl - int(left) <= 2 * look else CLIP_WORD",
   "new": "                    clipped = CLIP_WORD"
  },
  {
   "name": "낱말이 어디서 시작하든 띠가 자른 것으로 친다",
   "file": "axis_reader.py",
   "old": "                    clipped = CLIP_STRIP if wl - int(left) <= 2 * look else CLIP_WORD",
   "new": "                    clipped = CLIP_STRIP"
  },
  {
   "name": "두 배율의 잘림 판정은 먼저 본 것이 이긴다",
   "file": "axis_reader.py",
   "old": "    if CLIP_STRIP in (a, b):\n        return CLIP_STRIP\n    return a or b",
   "new": "    return a or b"
  },
  {
   "name": "안쪽 축선을 프레임 변에서부터 찾는다",
   "file": "axis_reader.py",
   "old": "    for x in range(min_offset, sub.shape[1]):",
   "new": "    for x in range(sub.shape[1]):"
  },
  {
   "name": "짧은 세로선도 축선으로 친다",
   "file": "axis_reader.py",
   "old": "        if _longest_run(sub[:, x]) >= run * h:",
   "new": "        if _longest_run(sub[:, x]) >= 1:"
  },
  {
   "name": "안쪽 축선 중 가장 긴 것을 고른다",
   "file": "axis_reader.py",
   "old": "    for x in range(min_offset, sub.shape[1]):\n        if _longest_run(sub[:, x]) >= run * h:\n            return x0 + x\n    return None",
   "new": "    cand = [x for x in range(min_offset, sub.shape[1]) if _longest_run(sub[:, x]) >= run * h]\n    return x0 + max(cand, key=lambda x: _longest_run(sub[:, x])) if cand else None"
  },
  {
   "name": "띠에서 잘린 낱말도 받는다",
   "file": "axis_reader.py",
   "old": "                if clipped:\n                    continue\n                out.append((value, at))",
   "new": "                out.append((value, at))"
  },
  {
   "name": "띠를 눈금 자리까지 끌어온다",
   "file": "axis_reader.py",
   "old": "    reach = tick_reach(dark, spine_x, y0, y1, cap=max(10, (y1 - y0) // 8))\n    band = label_band(dark, (spine_x, x1, top, bottom), spine_x - reach, top, bottom)",
   "new": "    reach = 0\n    band = label_band(dark, (spine_x, x1, top, bottom), spine_x - reach, top, bottom)"
  },
  {
   "name": "눈금을 물어도 첫 읽기를 그대로 쓴다",
   "file": "axis_reader.py",
   "old": "        if reach <= _TUNED_GAP_MAX:\n            return first",
   "new": "        if True:\n            return first"
  },
  {
   "name": "눈금이 짧아도 매번 다시 읽는다",
   "file": "axis_reader.py",
   "old": "        if reach <= _TUNED_GAP_MAX:",
   "new": "        if reach < 0:"
  },
  {
   "name": "눈금 너머 읽기가 사다리가 아니어도 쓴다",
   "file": "axis_reader.py",
   "old": "        return past if richer_read(first, past) else first",
   "new": "        return past"
  },
  {
   "name": "눈금 너머 읽기가 더 풍부하지 않아도 쓴다",
   "file": "axis_reader.py",
   "old": "    if ladder_size(second) <= ladder_size(first):\n        return None",
   "new": "    if False:\n        return None"
  },
  {
   "name": "라벨 수만 늘면 더 풍부한 읽기로 친다",
   "file": "axis_reader.py",
   "old": "    for value, row in first:\n        same = [v for v, r in second if abs(r - row) <= 3]\n        if same and same[0] != value:\n            return None\n    return second",
   "new": "    return second"
  },
  {
   "name": "사다리가 버린 라벨도 세어 준다",
   "file": "axis_reader.py",
   "old": "    return sum(1 for _v, row in pairs if lo <= row <= hi)",
   "new": "    return len(pairs)"
  },
  {
   "name": "더 큰 배율만 있어도 다시 읽는다",
   "file": "axis_reader.py",
   "old": "    return [t / float(height) for t in _GLYPH_TARGETS if t / float(height) < current]",
   "new": "    return [t / float(height) for t in _GLYPH_TARGETS]"
  },
  {
   "name": "글자 높이를 재지 못해도 배율을 만든다",
   "file": "axis_reader.py",
   "old": "    if not height:\n        return []",
   "new": "    if False:\n        return []"
  },
  {
   "name": "목표 크기는 하나뿐이다",
   "file": "axis_reader.py",
   "old": "_GLYPH_TARGETS = (40.0, 28.0)",
   "new": "_GLYPH_TARGETS = (40.0,)"
  },
  {
   "name": "작은 배율의 답이 사다리가 아니어도 쓴다",
   "file": "axis_reader.py",
   "old": "        smaller = past_the_ticks(sc)\n        if ladder(smaller)[0]:\n            return smaller",
   "new": "        smaller = past_the_ticks(sc)\n        if True:\n            return smaller"
  },
  {
   "name": "작은 배율로는 다시 읽지 않는다",
   "file": "axis_reader.py",
   "old": "    for sc in smaller_scales(label_height(dark, box, spine_x - reach, top, bottom),\n                             scale_for(y1 - y0, scale)):",
   "new": "    for sc in []:"
  },
  {
   "name": "사다리가 아닌 읽기도 크기가 있다",
   "file": "axis_reader.py",
   "old": "    if not ok:\n        return 0",
   "new": "    if False:\n        return 0"
  }
 ]
}
